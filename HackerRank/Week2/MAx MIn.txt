 public static int maxMin(int k, List<Integer> arr) {
    // Write your code here
Collections.sort(arr);
    int n = Integer.MAX_VALUE;
    for (int i = 0; i <= arr.size() - k; i++) {
        int c = arr.get(i + k - 1) - arr.get(i);
        n = Math.min(n, c);
    }
    return n;
    }
